import React, { useState, useEffect } from 'react'; import axios from 'axios'; function Feed() { const [posts, setPosts] = useState([]); useEffect(() => { async function fetchPosts() { try { const response = await axios.get('/api/feed'); setPosts(response.data); } catch (error) { console.error(error); } } fetchPosts(); }, []); return (<div> <h1>Feed</h1> <div> {posts.map((post, index) => ( <div key={index}> <h2>{post.user}</h2> <p>{post.content}</p> <img src={post.image} alt="Post" style={{ maxWidth: '300px', maxHeight: '300px' }} /> </div> ))} </div> </div>); } export default Feed;