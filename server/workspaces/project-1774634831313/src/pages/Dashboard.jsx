import React, { useState, useEffect } from 'react'; import axios from 'axios'; function Dashboard() { const [files, setFiles] = useState([]); useEffect(() => { async function fetchFiles() { try { const response = await axios.get('/api/files'); setFiles(response.data); } catch (error) { console.error(error); } } fetchFiles(); }, []); return (<div> <h1>Dashboard</h1> <form onSubmit={(e) => { e.preventDefault(); // Add file upload logic here }}> <input type="file" required /> <button type="submit">Upload</button> </form> <div> {files.map((file, index) => ( <div key={index}> <p>{file.name}</p> <img src={file.url} alt={file.name} style={{ maxWidth: '300px', maxHeight: '300px' }} /> </div> ))} </div> </div>); } export default Dashboard;